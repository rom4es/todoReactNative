// Общие стили для всего приложения

// Строка с кнопками
export const actions = {
  flexDirection: 'row',
  flexWrap: 'wrap',
  marginTop: 5,
};

// Кнопка
export const button = {
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center',
  height: 50,
  borderRadius: 50,
  backgroundColor: '#350b1f',
};

// Кнопка с иконкой
export const buttonIcon = {
  maxWidth: 50,
};

// Кнопка с отступом
export const buttonMargin = {
  marginRight: 10,
};

// Иконка кнопки
export const buttonIconImage = {
  width: 25,
  height: 25,
};

// Текст кнопки
export const buttonText = {
  color: '#FFF',
  fontSize: 18,
  fontWeight: 'bold',
};

// Селект
export const selectWrapper = {
  marginTop: 0,
  marginBottom: 20,
};

export const selectText = {
  marginBottom: 4,
  fontSize: 16,
  fontWeight: 'bold',
};

export const select = {
  borderWidth: 1,
  borderColor: '#350b1f',
  backgroundColor: '#d6ccd5',
  borderRadius: 10,
};

export const bold = {
  fontWeight: 'bold',
};
